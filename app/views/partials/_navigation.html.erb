    <nav class="navbar navbar-inverse">
      <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header center-text">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to "Technically Employed", root_path, class: "navbar-brand" %>
        </div>
        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <ul class="nav navbar-nav">
            <ul class="nav navbar-nav">
              <li class="active"><%= link_to "Jobs", jobs_path %></li>
            </ul>
          </ul>
          <ul class="nav navbar-nav navbar-right">
            <% if current_user %>
              <li class="active"><%= link_to "Logged in as #{current_user.username}", dashboard_path %> </li>
            <% end %>
            <% if !current_user %>
              <li class="active"><%= link_to "Login", login_path %></li>
            <% else %>
              <li class="active"><%= link_to "Logout", logout_path, method: :delete %></li>
            <% end %>
            <li class="active"><%= link_to "View Profile", cart_path %></li>
            <% if !current_user %>
              <li class="active"><%= link_to "Create Account", signup_path  %></li>
            <% end %>
          </ul>
        </div><!-- /.navbar-collapse -->
      </div><!-- /.container-fluid -->
    </nav>
