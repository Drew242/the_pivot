    <nav class="navbar navbar-inverse">
      <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header center-text">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to "Technically Employed", root_path, class: "navbar-brand" %>
        </div>
        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <ul class="nav navbar-nav">
              <li class=""><%= link_to "View Jobs", jobs_path %></li>
              <% unless current_user && current_user.company %>
                <% if current_user %>
                  <li><%= link_to "Post Jobs", new_company_path %></li>
                <% else %>
                  <li><%= link_to "Post Jobs", login_path %></li>
                <% end %>
              <% end %>
          </ul>
          <ul class="nav navbar-nav navbar-right">
              <li class="" id="favorite_jobs"><%= link_to "Favorite Jobs", cart_path %></li>
            <% if current_user %>
              <% if company_admin? %>
                <li class="" id="profile"><%= link_to "View Company Profile", companies_admin_company_path(current_user.company.id) %></li>
              <% end %>
              <li class="" id="profile"><%= link_to "View Profile", user_path(current_user) %></li>
              <li class=""><%= link_to "#{current_user.username}", dashboard_path %> </li>
              <li class=""><%= link_to "Logout", logout_path %></li>
            <% else %>
              <li class=""><%= link_to "Create Account", signup_path  %></li>
              <li class=""><%= link_to "Login", login_path %></li>
            <% end %>
          </ul>
        </div><!-- /.navbar-collapse -->
      </div><!-- /.container-fluid -->
    </nav>
